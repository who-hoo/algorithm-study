package 아더

fun main() = with(System.`in`.bufferedReader()){
  val split = readLine().split(" ")
  var N: Int = split[0].toInt()
  var M: Int = split[1].toInt()
  var board = Array(10) { IntArray(10) }

  for (i in 0 until 10) {
    for (j in 0 until 10) {
      board[i][j] = 
    }
  }
}
